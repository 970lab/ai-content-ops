interface:
  display_name: "AI Content Operations"
  short_description: "Coordinate traceable team handoffs from source to feedback."
  default_prompt: "Use $ai-content-ops to map our content team into a local-first, approval-gated workflow with traceable artifacts."
policy:
  allow_implicit_invocation: true
